server:
  port: 3344
spring:
  application:
    name: springcloud-config-server
    #远程连接仓库
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/wang_sen006/springcloud-config.git